name: tests

on:
  push:
  pull_request:

jobs:
  unittest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install -r requirements.txt
      - run: python -m py_compile main.py search_logic.py
      - run: python -m unittest discover -s tests -v
      - run: python -c "import yaml; data = yaml.safe_load(open('metadata.yaml', encoding='utf-8')); assert data['name'] == 'astrbot_plugin_jrsq' and data['version']"
      - run: python -m json.tool data/plugin_config.json > /dev/null
